package Class2;

public class Variables {
    public static void main(String[] args) {
        /*
        Below line is reserving a box in computer memory.
        we are calling it box1 and if we need the information that we are storing inside of this box,
        we can simply say to computer to give us the contents of box1.
        int= what of type vof data we want to store
        box1= is the name of the box
        = sign will take whatever we write after and will store it inside of box1


        *\
         */
        int box1=10;
        System.out.println(box1);
        //if we are printing something from a box we dont need ""

        /*To store whole numbers we have 4 different types of boxes.
        1) byte 2) short 3) int 4) long

         */

        short myBox=10000;
        System.out.println(myBox);


        boolean hungry=true; //true means yes
        boolean sleepy=false; //false means no


    }
}
